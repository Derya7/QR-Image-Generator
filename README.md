### QR-Image-Generator
With this software you can create an qr image of any website that you'd like

## 🚀 Run it with Docker
If you have Docker installed, you can get the website running in seconds:

Open your terminal inside the project folder.

Build the image:
# docker build -t qrcode .

Start the container:
# docker run -d -p 8080:80 qrcode

## How to Run
Once it's running, just head over to http://localhost:8080 in your browser.
